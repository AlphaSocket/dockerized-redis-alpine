#!/bin/sh
#

# exit if a command fails
set -e

# Install dependencies
apk add --no-cache $SETUP_DEPENDENCIES_CONFIG $SETUP_DEPENDENCIES_SETUP

# Setup config folder
if [ ! -a "$SETUP_PATHS_CONFIG_REDIS" ]; then
    mkdir -p $SETUP_PATHS_CONFIG_REDIS;
fi


exit 0